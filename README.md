# 💨 VapeShop - Telegram Бот с Mini App

Полнофункциональный Telegram бот для продажи жидкостей, подов и расходников с красивым Mini App.

## 🚀 Возможности

### Для пользователей:
- 🛒 **Каталог товаров** с категориями (жидкости, поды, расходники, наборы)
- 📱 **Mini App** с современным дизайном
- 🛍️ **Корзина** с управлением количеством
- 📝 **Оформление заказа** с доставкой
- 👤 **Профиль** с историей заказов
- 📊 **Отслеживание статуса** заказа

### Для администраторов:
- 📊 **Статистика** магазина
- 📦 **Управление заказами** (подтверждение, отмена, статусы)
- 🛍️ **Управление товарами** через БД
- 🔔 **Уведомления** о новых заказах

## 📋 Требования

- Node.js 16+
- Telegram Bot Token (от @BotFather)

## ⚙️ Установка

1. **Установите зависимости:**
```bash
npm install
```

2. **Настройте переменные окружения:**
```bash
# Скопируйте файл .env.example в .env
cp .env.example .env

# Отредактируйте .env и укажите:
# - BOT_TOKEN (получите от @BotFather)
# - ADMIN_IDS (ваш Telegram ID)
# - WEB_APP_URL (URL вашего сервера)
```

3. **Запустите бота:**
```bash
npm start
```

## 🔧 Настройка

### Получение токена бота:
1. Откройте @BotFather в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте полученный токен в `.env`

### Узнайте свой Telegram ID:
1. Откройте @userinfobot
2. Отправьте `/start`
3. Скопируйте ID в `ADMIN_IDS` в `.env`

### Настройка Web App URL:
Для локальной разработки используйте ngrok:
```bash
ngrok http 3000
```
Скопируйте HTTPS URL в `WEB_APP_URL` в `.env

## 📁 Структура проекта

```
lol/
├── bot/
│   ├── index.js          # Основной файл бота
│   ├── database.js       # База данных SQLite
│   └── keyboards.js      # Клавиатуры
├── webapp/
│   ├── index.html        # Mini App HTML
│   ├── styles.css        # Стили
│   └── app.js            # Логика Mini App
├── package.json
├── .env.example
└── README.md
```

## 🎨 Функции Mini App

- **Адаптивный дизайн** под тему Telegram
- **Анимации** и плавные переходы
- **Категории** товаров с фильтрацией
- **Корзина** с сохранением состояния
- **Оформление заказа** в 1 клик
- **Haptic Feedback** для тактильной отдачи

## 🛠️ Админ-панель

Доступные команды для администраторов:

| Команда | Описание |
|---------|----------|
| 📊 Статистика | Общая статистика магазина |
| 📦 Заказы | Просмотр и управление заказами |
| 🔙 В меню | Возврат в главное меню |

## 📊 База данных

Таблицы:
- `users` - пользователи
- `categories` - категории товаров
- `products` - товары
- `orders` - заказы
- `order_items` - элементы заказов
- `cart` - корзина

## 🔔 Уведомления

При новом заказе администраторы получают:
- Информацию о заказе
- Список товаров
- Контакты покупателя
- Кнопки управления статусом

## 🚀 Деплой

### Heroku:
```bash
heroku create your-app-name
heroku config:set BOT_TOKEN=your_token
heroku config:set ADMIN_IDS=your_id
heroku config:set WEB_APP_URL=https://your-app.herokuapp.com
git push heroku main
```

### VPS:
```bash
# Установите PM2
npm install -g pm2

# Запустите
pm2 start bot/index.js --name vapeshop

# Автозапуск
pm2 startup
pm2 save
```

## 📝 Лицензия

ISC

## 💡 Советы

1. Добавьте реальные изображения товаров в БД
2. Настройте HTTPS для продакшена
3. Регулярно делайте бэкапы базы данных
4. Используйте webhook вместо polling для продакшена

## 🤝 Поддержка

По вопросам обращайтесь: @vapeshop_support
